const getId = (ele) => document.getElementById(ele);
const getClass = (ele) => document.querySelector(ele);
const getAllClass = (ele) => document.querySelectorAll(ele);

export { getId, getClass, getAllClass };
